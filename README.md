# AzureSubnetCalculator
 This script calculates Azure-compliant subnet details including:     - Network address     - Broadcast     - Subnet mask     - Usable host range     - Azure reserved IPs (gateway, reserved .2 and .3)     - Azure-usable hosts count     - Optional subnet splitting     - Optional hosts-to-CIDR recommendation
